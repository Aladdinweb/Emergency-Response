<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Foreground service (connection status indicator) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- GSM/SMS fallback -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- In-app update installer -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".EmergencyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IlineEmergency"
        tools:targetApi="34">

        <!-- allowBackup="false": StaffMemberEntity/AlertLogEntity data plus the
             sms_fallback_contacts phone-number directory shouldn't ride along
             in an Auto Backup blob to the user's Google account by default
             for an app handling institutional/security data. Revisit if a
             deliberate encrypted-backup strategy is designed later. -->

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- FCM -->
        <service
            android:name=".fcm.EmergencyMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".fcm.AlertAckReceiver"
            android:exported="false" />

        <!-- DND-bypass alarm + foreground service -->
        <receiver
            android:name=".alert.AlertBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ilinetech.emergency.action.TRIGGER_ALERT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.ConnectionForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- SMS fallback: binary data-SMS carries routing metadata, invisible
             in normal messaging apps. See SmsPayloadBuilder / SmsDispatcher /
             SmsDataPayloadReceiver — this replaced a plain-text-SMS receiver
             after device testing showed the routing codes leaking into the
             visible message body. -->
        <receiver
            android:name=".sms.SmsDataPayloadReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.DATA_SMS_RECEIVED" />
                <data android:scheme="sms" android:port="6474" />
            </intent-filter>
        </receiver>

        <!-- Quick toggle from the connection-status notification -->
        <receiver
            android:name=".service.AppToggleReceiver"
            android:exported="false" />

        <!-- In-app updates -->
        <receiver
            android:name=".core.update.UpdateDownloadReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
